<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.GridLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:compat="http://schemas.android.com/apk/res-auto"
	android:layout_width="wrap_content"
	android:layout_height="wrap_content"
	compat:columnCount="3">

	<TextView
		style="@style/TextLabel"
		android:text="@string/powFormula"
		compat:layout_columnSpan="3"/>

	<TextView
		style="@style/TextLabel"
		android:labelFor="@+id/decPow"
		android:text="@string/powP"/>

	<EditText
		android:id="@+id/decPow"
		style="@style/SignedDecimal"
		android:ems="6"
		android:hint="@string/power">

		<requestFocus/>
	</EditText>

	<TextView
		style="@style/TextLabel"
		android:text="@string/watts"/>

	<TextView
		style="@style/TextLabelCenter"
		android:text="@string/eq"
		compat:layout_columnSpan="2"/>

	<Space/>

	<TextView
		style="@style/TextLabel"
		android:labelFor="@+id/decCur"
		android:text="@string/powI"/>

	<EditText
		android:id="@+id/decCur"
		style="@style/SignedDecimal"
		android:ems="6"
		android:hint="@string/current"/>

	<TextView
		style="@style/TextLabel"
		android:text="@string/amps"/>

	<TextView
		style="@style/TextLabel"
		android:labelFor="@+id/decVolt"
		android:text="@string/powV"/>

	<EditText
		android:id="@+id/decVolt"
		style="@style/SignedDecimal"
		android:ems="6"
		android:hint="@string/voltage"/>

	<TextView
		style="@style/TextLabel"
		android:text="@string/volts"/>

	<Button
		android:id="@+id/btnClear"
		android:layout_width="wrap_content"
		android:layout_height="wrap_content"
		android:layout_marginTop="15dp"
		android:text="@string/clear"
		compat:layout_columnSpan="3"/>
</android.support.v7.widget.GridLayout>
